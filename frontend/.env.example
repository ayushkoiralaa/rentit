# Leave blank to use Vite's dev proxy (see vite.config.js) which forwards
# /api and /uploads to http://localhost:5000. Set this only when pointing
# the built frontend at a deployed backend on a different origin.
VITE_API_URL=
